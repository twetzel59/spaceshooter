use sfml::graphics::*;
use sfml::system::Vector2u;

use resources::Resources;

pub struct Enemy<'s> {
    sprite: Sprite<'s>
}

impl<'s> Enemy<'s> {
    /*pub fn new(res: &'s Resources, win_size: &Vector2u) -> Enemy<'s> {
        
    }*/
}
